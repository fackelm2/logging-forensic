[build-system]
requires = ['setuptools>=75,<83', 'wheel']
build-backend = 'setuptools.build_meta'

[project]
name = 'logging-forensic'
version = '0.2.1'
description = 'Forensic logging setup for python projects'
authors = [{ name = 'Dietmar Fackelmann', email = 'logging-forensics@nmit.de'}]
license = 'MIT'
readme = 'README.md'
requires-python = '>=3.10'
dependencies = []

[project.urls]
Homepage = 'https://github.com/fackelm2/logging-forensic'

[tool.setuptools.packages.find]
where = ['.']